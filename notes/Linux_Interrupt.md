##About Mask

`mask_irq` VS `disable_irq`

The masked IRQs would not be discarded, once unmasked, PIC send them to CPU.

`IRQF_ONESHOT` make no sense for edge irq?

##Important Structures

`irq_domain`
`irq_chip`
`irq_desc`
`irqaction`

